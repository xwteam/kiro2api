# Changelog

本文件记录项目的所有重要变更。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

## [0.10.1] - 2026-08-09

继续按同类实现逐条对齐。本版补的是 v0.10.0 漏掉的那几处**身份字段**,以及一个此前
"界面说配好了、实际全程直连"的功能。

### Added

- **出站代理,按账号分流**。凭据级 `proxyUrl`(含 `proxyUsername`/`proxyPassword`)、
  全局 `proxyUrl`,优先级 **凭据级 > 全局 > 直连**;凭据级填 `"direct"` 表示该账号显式
  直连(即便全局配了)。支持 `http` / `https` / `socks5`。

  此前管理接口**收下这三个字段就丢**,状态里的 `hasProxy` 还硬编码 `false` —— 用户在
  面板上填了代理、界面显示保存成功,实际全程直连。现在真正落库并生效,`hasProxy` 也报真值。

  纪律:**同一个账号的数据面、令牌刷新、余额查询、模型清单、后台续期一律走同一个出口**。
  数据面走代理而刷新从主 IP 出,比不配代理更糟 —— 那等于主动告诉上游这两条流量属于同一
  个中转。客户端按"种类 + 代理"分桶缓存,不会每请求重建。

  代理地址无法解析时**大声报错并直连**,不静默吞掉:静默直连会让运营者以为流量走了代理。

### Fixed

- **`conversationId` 每个请求都重新生成,而且不是 UUID 形状**(此前是 32 位无连字符
  十六进制)。上游看到的是"这个账号每发一句话就开一段全新对话"。现在优先从客户端
  `metadata.user_id` 里提取 session UUID —— 真实客户端把会话标识放在那里,于是同一次
  会话的多个请求共用同一个 `conversationId`;取不到才新生成一个 UUID。

- **完全没有发 `agentContinuationId`**。真实客户端每个请求发一个新的 UUID,现已补上。

- **`ConversationState` 的字段顺序**与真实客户端不一致(JSON 键序是可观测的),已对齐。

- **管理面新增的账号不会冻结 machineId**。v0.10.0 的冻结只在启动时对**文件里已有**的
  凭据跑一遍,而运营者恰恰是靠管理面加号的 —— 后加的每个号照旧每请求现算,又回到
  "每刷新一次换一台机器"。现在入池那一刻就冻结,所有新增路径(单条 / 批量导入 /
  SSO 换取)统一覆盖。

- **`isCurrent` 硬编码 `false`**。粘滞选号之后"现在在用哪个号"是真实存在且运营者需要
  看见的状态,面板据它能一眼看出流量集中在谁身上。现报真值。


## [0.10.0] - 2026-08-09

对照一个长期稳定运行的同类实现做了逐模块比对,并用探针抓取真实字节核对。结论是此前
两版(0.9.0/0.9.1)对封号原因的归因**站不住**:那份稳定实现既复用连接、也不锁 HTTP/1.1、
TLS 后端还默认 rustls —— 我们赌的三件事它一件都没做。真正的差异在**行为形态**和
**几个形状不对的字段**上,本版逐条对齐。

### Changed

- **`priority` 负载模式由"每请求轮换"改为"粘住当前账号"**。

  同名的模式,语义此前是反的:我们每次请求 `cursor+1` 换一个账号,于是上游在同一个出口
  IP 上看到几百个不同 machineId 在**秒级交替**出现 —— 那是账号池最容易被识别的形状。
  真实客户端永远是"一台机器、一个账号、连续干活"。现在一个账号一直用到它被判不可用,
  才切下一个,切过去之后同样粘住。`balanced` 模式不变(仍按权重铺开)。

- **额度耗尽 / 连续鉴权失败的账号不再回池**。

  此前 402 进 30 分钟冷却、401/403 进 5 分钟冷却,到点自动回到轮转里。后果是一个**已被
  上游封停**的账号每 5 分钟被重新拿去打一次、永不停止;一个月度额度耗尽的账号每 30 分钟
  去撞一次必然失败的墙(一天 48 次)。几百个号叠起来就是一条持续不断的 401/403/402
  背景流,正是"批量凭据校验"的形状。现在这两类一律停止使用。

  停用只置**内存态**:重启或在管理面点"重置"即复活,不会把上游一次权限抖动变成永久损失。

- **429 从"配额耗尽"改判为"瞬时限流"**。429 是上游临时限流,退避重试即可恢复,而限流
  恰恰是高峰期最常见的响应。此前把它和 402 一起判成配额,一次限流就让账号吃长冷却。

- **数据面端点由三个收敛为一个**。另外两个(`codewhisperer.{region}` 主机、把
  `AmazonQDeveloperStreamingService.SendMessage` 打在 `/generateAssistantResponse` 上的变体)
  真实客户端从不产生,后者本身也不自洽(`x-amz-target` 是 awsJson 协议的头,真实客户端
  配它时路径是 `/`)。更糟的是它们何时被用:一收到 429 就**立刻拿同一个账号的令牌**去打
  这两个入口 —— 那是探测行为的形状。旧配置里写着 `codewhisperer`/`amazonq` 仍被接受,
  一律解析到唯一的真实端点。

### Fixed

- **令牌刷新请求此前不带任何身份头 —— 连 `User-Agent` 都没有**(实测字节确认)。

  而 Social 的刷新端点是 Kiro **自家**的服务,日志完全在他们手里;刷新又是**每个账号都
  必然走**的路径。数据面伪装得再像,这里先把身份交代了。现在两条链路分别按真实客户端的
  形态补齐:Social 走 axios 形态(`Accept: application/json, text/plain, */*`、
  `User-Agent: KiroIDE-<版本>-<machineId>`、`Accept-Encoding: gzip, compress, deflate, br`),
  IdC 走 sso-oidc 的 aws-sdk-js 形态(SDK 版本 3.980.0,尾部不带 machineId);两者都补
  显式 `host` 与 `Connection: close`。

  声明了 `Accept-Encoding` 就必须真解得开,故 reqwest 开启 gzip/brotli/deflate;同时给
  数据面与其余控制面客户端显式关掉自动补该头(真实 aws-sdk-js on Node 不发)。

- **machineId 每刷新一次就变一个**。它由 `refreshToken` 现算,而刷新响应会回带新的
  refreshToken(我们照收覆盖)—— 于是同一个账号不断更换设备指纹,真实设备绝不会这样。
  现在在载入时一次性派生并**冻结写回凭据**,此后不随轮换漂移。配置里设了全局 `machineId`
  时不冻结(那是"整池共用一个身份"的开关,固化会让日后改配置失效)。

- **ksk(API Key)账号的 machineId 退化成一个全局常量**。按 ksk 派生的那条路径
  (`sha256("KiroAPIKey/" + ksk)`)是死代码,而 ksk 账号没有 refreshToken(空串),
  于是全部落到 `sha256("KotlinNativeAPI/")` —— 所有 ksk 账号对上游自称同一台机器,
  且该常量在任何人的部署里都一样。现按凭据类型互斥分流,两条路不互相回落;材料都缺时
  用按凭据 id 稳定的兜底,绝不发出常量指纹。

- **同一个账号在不同链路上报出两个不同的 machineId**。`impersonation_for` 此前被复制了
  三份,只有数据面那份认配置级 `machineId`,余额查询与模型清单那两份不认。现在收口到
  唯一入口 `Impersonation::for_credential`。

- **`amz-sdk-invocation-id` 不是 UUID**。此前发的是 32 位无连字符十六进制;aws-sdk-js
  这个头**永远**是 UUID v4,而它出现在每一个请求上。现按 RFC 4122 生成标准写法。

- **数据面请求头顺序与真实客户端不一致**,且 `host` 因为没有显式设置被 hyper 追加到了
  倒数第二位。头顺序是与 TLS 指纹并列的识别维度。现已逐项对齐并用探针抓字节核对
  (顺序完全一致)。

- **余额查询与模型清单对 ksk 凭据发空 Bearer**:它们直接读 `access_token`,而 ksk 的令牌
  在 `kiroApiKey` 里,于是这两条链路对 ksk 账号必然 401/403。现改走 `bearer()` 并配套
  `tokentype: API_KEY`;两者同时补 `Connection: close`。

- **`claude-opus-5` 没有映射分支,被静默降级成 Opus 4.6**。请求方要的是 5,拿到的是 4.6,
  且毫无提示。

- **`usage_summary_range_and_hours_and_invalid` 在 CI 上间歇性失败**:夹具把记录放在整 1 小时的
  窗口边界上,而查询侧会自己重新取一次 `now`,测试跨过 1 秒该记录就掉出窗口。被测代码正常,
  是用例在赌时序。现留 60 秒余量。

- **SSE 下行没有任何保活**。`ping()` 写了却全项目无人调用,`Sse::new()` 也没挂 keep-alive。
  上游长推理时会有几十秒不出字节,中间的 CDN/反代/客户端读超时会把连接掐掉,表现成
  "会话莫名其妙断了"。四条协议的流式响应现在统一 25 秒一个保活帧。


## [0.9.1] - 2026-08-09

### Fixed

- **上游连接锁到 HTTP/1.1**。真实客户端是 aws-sdk-js on Node,其 HTTP handler 默认走 1.1、不主动升 h2;而我们经 ALPN 协商到的是 **h2**(实测)。协议版本本身就是可识别的差异。

  更要紧的是:`Connection: close` 是 HTTP/1.1 的头,**h2 明确禁止它** —— v0.9.0 加了那个头却没锁协议,于是它**只是摆设**。实测已确认:锁定后出站协议从 `HTTP/2` 变为 `HTTP/1.1`。

- **TLS 后端默认改为 native-tls(vendored OpenSSL)**。真实客户端是 Electron/Node,TLS 握手走 OpenSSL,其 ClientHello(JA3)与 rustls 截然不同。**TLS 指纹在任何 HTTP 内容发出之前就暴露**,是最标准的机器人识别手段之一,而我们此前只有 rustls。关掉 `native-tls-backend` 特性可回落 rustls(编译更快、不需要 OpenSSL 工具链,但指纹对不上)。

- **machineId 支持配置级兜底**。优先级由两级扩为三级:凭据自带 > 配置 `machineId` > 由 refreshToken 派生。配置级适合"一台机器、一个身份"的部署形态;不设则维持每账号各自派生(默认,与真实客户端"一账号一机器"一致)。

- **构建镜像与运行镜像的 glibc 世代不一致,产出的二进制启动即崩溃**。构建阶段用的浮动标签 `rust:1-slim` 已随上游滚到 Debian trixie(glibc 2.41),运行镜像仍是 bookworm(2.36)。以前用 ring 没暴露,本版换 vendored OpenSSL 后,它的 C 代码会用到 glibc 2.38 才加入的 `strlcpy`/`strlcat`,于是两个架构的二进制都被打上 `GLIBC_2.38` 依赖,容器启动即 `version GLIBC_2.38 not found` 并反复重启。

  这类失败**编译、镜像构建、推送全绿**,只在容器启动那一刻炸。现已把构建镜像钉到 `rust:1-slim-bookworm`,并把"两边必须同一 Debian 世代"钉成测试断言(`tests/dockerfile_libc.rs`),换任一边都会先红在测试上,而不是先红在生产上。

### Changed

- `stats::persist` 的刷盘测试改为轮询等待期望内容(最长 5 秒),不再等固定时长后断言。原写法在 CI 上会因调度被抢占而间歇性失败,而被测代码完全正常。

## [0.9.0] - 2026-08-09

### Fixed

- **整池账号被上游以 `security precaution` 成批封停:同一条 TCP 连接上轮换了多个账号身份**。中转走 reqwest 连接池(idle 90 秒),于是同一条 TCP/TLS 连接上会依次发出不同账号的令牌,而每个账号在 user-agent 里还各自声称是不同的机器(machineId 随凭据变化)。真实的 Kiro 客户端不可能这样 —— 一台机器、一个账号、一条连接。**「同一条连接上轮换多个身份」是账号共享/盗用最直接的证据**,比「同 IP 多账号」强得多:后者还能用 NAT 或公司网络解释,前者解释不了。

  症状与此吻合:线上 1046 个账号烧到只剩 22 个健康,而取三个**从未经过中转**的账号直接查上游余额全部正常(`KIRO FREE`,额度未动)—— **账号不是进来就死的,它们活着,直到被中转拿去发请求**。另一位用户在**不同 IP、不同账号来源**的独立部署上报告了同样症状,共同因素只剩本软件的 wire 行为。

  现在数据面请求带 `Connection: close`,且数据面与控制面客户端都设 `pool_max_idle_per_host(0)`。代价是每次请求多一次 TLS 握手 —— 相比把整池账号暴露在同一条连接上,这个代价值得。

  同时把 user-agent 的 SDK 版本从 `1.0.27` 对齐到 `1.0.34`(与被观测的真实客户端一致;版本号本身也是指纹的一部分)。

- **瞬态失败换账号前加指数退避**(200ms→2s + 抖动),避免上游抖动时多个并发请求同拍重试、把故障放大成尖峰。**账号级失败(令牌失效/额度耗尽)不退避** —— 换个账号立刻就能成,等待只是拖慢用户。此分工照观测。

### 关于诊断过程

初版修复基于一个**错误结论**:以为"一秒内跨账号重试 3 次"触发了风控,做了封禁熔断器 + 换号延迟。与同类实现逐行对比后该结论被**证伪** —— 存在换账号更快、重试次数更多、对 `suspend` 零特殊处理却长期稳定的实现;若"换得快"是病因,它应当烧得更快。熔断器因此**整个作废并回退**:保留一个基于已证伪理论的机制,只会变成以后没人看得懂的包袱,而且它确实会挡掉本可成功的请求。

真正的差异在 wire 层:真实客户端每个请求都是独立连接,而我们复用连接。

## [0.8.1] - 2026-08-03

### Fixed

- **面板上没有地方填 Kiro API Key**。v0.8.0 把后端与接口都做好了,却**没给「新增账号」表单加输入框** —— 于是从界面看,这个功能等于不存在:用户点开新增,只有 refreshToken / authMethod / clientId 三个框,ksk 无处可填,只能去敲 curl。

  现在认证方式下拉多一项 **API Key (`ksk_…`)**;选中它时显示 ksk 输入框,并**隐藏 refreshToken 那一栏**(这类凭据本就没有 refreshToken,留着一个必填框只会让人以为自己漏填了东西),提交时也不再要求它。词条 5 种语言齐备。

## [0.8.0] - 2026-08-03

### Added

- **支持用 Kiro API Key(`ksk_…`)导入账号**。这类凭据与 social/idc 根本不同:**key 本身就是数据面 bearer**,不换取令牌、不刷新、不过期,故完全不走 OAuth 刷新链路。

  两种导入方式:

  ```json
  {"kiroApiKey": "ksk_xxx", "authMethod": "api_key"}
  ```

  或调导入接口传 `kiroApiKey`(也认 `ksk` 别名);给了 key 就按 API Key 处理,不看 `authMethod` 写了什么 —— 显式声明 idc 却带着 key 会掉进"必填 clientId/clientSecret"的校验,把一个本来完整的凭据判成缺字段。

  实现按观测对齐:请求带 `tokentype: API_KEY` 头(缺了上游会按 OAuth 令牌解析这枚 key 并拒绝,且错误信息不提令牌类型,看起来像"这个 key 是坏的");machineId 用 `sha256("KiroAPIKey/" + ksk)`,与 OAuth 那条盐不可互换;`ensure_fresh` 与 `force_refresh` 对这类凭据显式短路;`is_expired`/`expires_soon` 恒答否 —— 否则缺省的 `expiresAt=0` 会被读成"1970 年就过期了",账号一进池就被判死。

- **配置自相矛盾的 API Key 凭据入池即禁用**。声明了 `authMethod=api_key` 却没给 `kiroApiKey` 时,该凭据取不到 bearer,又被判定为 API Key 凭据(故不刷新),留在池里只会在跨账号重试里反复空转 —— 每次被选中都在同一处失败。现在加载阶段就判出来并禁用,且**「重置」拒绝救活它**:重置只清 strike/冷却/结论,改不了配置本身,复活后立刻重新走回同一条错误路径。出路是改对配置再重启。(照观测补齐。)

## [0.7.14] - 2026-08-03

### Fixed

- **首页「全局剩余积分」经常空白,要点一次刷新才有数**。聚合端点只累加**仍新鲜**(5 分钟 TTL)的缓存条目,于是**只要超过 5 分钟没打开过账号页,首页就是空的** —— 盘上明明躺着全部 253 个账号的余额,却因为"不新鲜"整个不显示。

  病根是把两件事混成了一件:`is_fresh` 该回答的是「**要不要去上游重查**」,不是「**要不要显示**」。余额不会分钟级剧变,6 分钟前的值完全有参考意义;把它藏起来逼用户点刷新,**反而制造了一次本可避免的上游调用** —— 与这份缓存存在的理由正好相反。

  现在展示读取取缓存里**全部**条目、不按 TTL 过滤;`oldestCacheUnix` 原样带出,界面据此说明数据有多旧。新鲜度判定本身不变,仍用于决定要不要重查。

### Added

- **活跃账号的令牌提前续期**。近 24 小时内被选中过的账号,令牌剩余不足 10 分钟时由后台提前续上,请求不必再等一次刷新。

  **刻意只续活跃账号。** 直觉解法是"定时把全池令牌都续上",但那会给每个账号造出一条**永不停歇的后台心跳**:253 个账号、令牌约 1 小时到期,即每小时 253 次上游调用、24 小时不停,背后没有任何用户请求。而本项目的账号已被上游以 `security precaution` 封过(写这段时池里躺着 24 个),"无人使用却持续产生规律流量"恰是风控最容易盯上的形状 —— 此前为「全局积分」定方案时,也正是出于同一顾虑明确要求不加定时刷新。

  故提前续期**与真实使用量挂钩**:闲置账号一次多余调用都不产生,上游流量正比于实际用量。另有三道闸:已禁用/冷却中/被封禁的账号不续(它们本就不该被选中);单轮最多 5 个(刚批量导入时大量令牌在相近时刻到期,不设上限会在同一拍打出几百个请求);刷新一律走 `ensure_fresh`,与数据面共用同一把 per-credential 单飞锁与失败处置,不另写一份。

## [0.7.13] - 2026-08-03

### Fixed

- **codex 的 502:中转自己造出了畸形请求**。上游硬性要求消息里出现 `toolUse`/`toolResult` 时 `toolConfig` 必须存在,否则整个请求以 `TOOL_CONFIG_MISSING` 被拒。而工具**可能在到达数据面前就被合法丢掉** —— Responses 的内置工具(`web_search`/`local_shell`/`file_search`)由 OpenAI 服务端执行、中枢无等价物,故转换时丢弃(v0.7.1);客户端某轮**只带内置工具**时,`tools` 就成了空数组,而对话历史里的工具调用还在。于是请求变成「有工具调用、没有工具定义」—— **这是本服务自己造的,不是调用方的错**。

  线上复现:带工具历史 + 仅内置工具 → `502`;同样历史带一个函数工具 → `200`。

  现在发往上游前,从对话历史收集所有出现过的工具名,凡当前 `tools` 未声明的补一份最小规格(空对象 schema)。补的是模型自己调用过的工具,补上只是让请求自洽;不补则整轮对话彻底失败。客户端显式声明的工具优先,同名不覆盖不重复。无工具且历史无工具调用时行为不变(不发 `toolConfig`、任务类型 `vibe`)。

- **`TOOL_CONFIG_MISSING` 归入确定性请求错误**。它此前落在 `Transient`,于是一个换任何账号都不可能成功的请求被跨账号重试 —— 半小时内 26 次错误波及 **25 个账号**,与 v0.7.12 修掉的超长请求是同一类漏网。现不重试、不冷却、不累计 strike,并以 400 回明确说明。这一档现在是**兜底**:转换层已不该再造出这种请求。

## [0.7.12] - 2026-08-03

### Fixed

- **一个超长请求会把整个账号池打伤,最终导致 503**。上游对超出长度上限的请求回 `400 {"reason":"CONTENT_LENGTH_EXCEEDS_THRESHOLD"}`,而确定性请求错误此前**只认 `INVALID_MODEL_ID`** 一种,这个码落进了 `Transient`(瞬时错误、值得重试)。后果是一个换任何账号都不可能成功的请求被**跨账号重试一遍,每换一个账号就给它记一次失败、攒一次 strike**。线上实测:一个下午把 253 个健康账号打成 149 个带伤、26 个进冷却、可用池缩水,随后开始回 `503 no available upstream account`。

  现归入 `InvalidRequest` —— 该档语义本就是「换账号无用」:不重试、不冷却、不累计 strike、不落账号失败日志。机制现成,此前只是没认出这个码。

- **客户端只看到 `502 upstream request failed`,不知道发生了什么**。上下文超长这件事本身是客观限制,难受的是它被讲成了一个毫无信息量的 502 —— 而且**这个会话从此必然失败**:客户端每轮重发完整对话历史,下一轮只会更长。用户唯一能试出来的办法是"重开一个会话"。现在回 `400` 并明说是长度超限、说明它不会自愈、该缩短上下文或新开会话。

- **`RelayError::InvalidModel` 更名为 `InvalidRequest`**。它开始承载"上下文超长"之后,旧名字本身就是个假陈述;文案也改为**按 reason 码分别给话** —— 笼统套一句"模型不可用"会让使用者去换模型,换多少次都没用。

## [0.7.11] - 2026-08-01

### Fixed

- **测试套件把临时目录漏在 `/tmp` 里,而且从不回收**。125 处测试各自拼路径(`std::env::temp_dir().join(format!("kiro2api_xxx_{pid}_{nanos}"))`),建完就不管了 —— 没有 RAII guard,没有收尾清理,进程退出即成孤儿。这些残留只增不减:排查一次磁盘打满事故时,`/tmp` **顶层**堆着 **9582 个**这样的目录,而它们只积累了 4 天(7/26–7/29 那轮发版测试)。systemd-tmpfiles 对 `/tmp` 的老化策略是 30 天,远慢于产生速度,靠它兜不住。

  代价不在体积 —— 9582 个目录合计仅 52M。代价在**污染**:`/tmp` 顶层被撑到近万条目录项之后,任何人或任何脚本去那里找东西都得先趟过这一万条,排查磁盘问题时尤其碍事(那次事故正是在这堆残留里翻找真正的元凶)。

  现改为所有测试临时产物收进**单一的 per-process 根目录** `/tmp/kiro2api-tests/<pid>/`,并在每个测试进程启动时(`Once`,进程内只跑一次)回收掉那些 **pid 已不在 `/proc` 里**的旧根目录。于是下一轮测试自动清掉上一轮,零手工维护,`/tmp` 顶层最多只多出 `kiro2api-tests` 这一个条目。只删已退出进程的根目录,所以并行跑多个测试二进制时互不误伤。

  新增 `src/test_tmp.rs`(仅 `#[cfg(test)]` 编译,不进产物)提供四个接口。**区分「唯一型」与「稳定型」是必须的,不是风格取舍**:老 helper 里有一批路径**不含纳秒**(`apikey::tmp_path`、`server::auth::tmp_store_path`、`stats::usage::test_dir` 等),用例靠「同一 tag 反复取到同一路径」来先写后读;若一律换成唯一路径,这些用例会读到空文件而挂掉。故唯一型用 `dir()` / `file()`,稳定型用 `stable_dir()`(每次取时清空)/ `stable_file()`(路径恒定、内容不动)。

  `file()` 刻意让每个临时文件独占一层目录:不少用例会在同一路径旁边写 `<name>.next-id`、`<name>.daily.json` 之类的**伴生文件** —— 那 9582 个残留里就有一千多个是这类伴生文件,放进各自的目录才不会互相踩,也才收得干净。

  `src/main.rs` 里还剩一处漏点改不掉:二进制 crate 看不见 lib 的 `#[cfg(test)]` 模块。影响可忽略(一轮测试一个文件)。

## [0.7.10] - 2026-07-29

### Fixed

- **发版后面板仍显示旧版本/旧行为**(例如后端已是 0.7.9,面板的「检查更新」却显示 0.7.6)。面板静态资源**一个缓存头都不发** —— 没有 `Cache-Control`、没有 `ETag`、没有 `Last-Modified`。HTTP 规定这种情况下浏览器可以**启发式缓存**,自己决定存多久,于是发版后用户拿到的仍是旧的 JS/HTML。

  这类现象最难查:服务端每个接口查出来都是对的(`/health`、`/api/admin/server-info`、`/api/admin/check-update` 三处当时全返回 0.7.9),问题只存在于浏览器手里的那份副本。本项目此前几次「改了却像没生效」的报告,大概率也有这一份原因。

  现在资源带 `Cache-Control: no-cache` + 基于内容 SHA-256 的强 `ETag`,并支持 `If-None-Match` → `304`。`no-cache` 不是「不缓存」,而是「可以存但每次必须回源验证」:内容没变回 304 不带 body,变了才传新内容 —— 既不会再有陈旧面板,也不必每次全量重传。

## [0.7.9] - 2026-07-29

### Fixed

- **「可用账号」把所有不健康的号都算了进去**(封禁、额度耗尽、令牌过期、续期被拒)。统计卡在前端复算 `accounts.filter(a => !a.disabled).length` —— 而这几类**都不是「禁用」**,它们的 `disabled` 恒为 false,于是全被算作可用。结果同一页上「封禁 6」与「可用 253」并列出现,两个数字互相打架。

  现改为**只数健康档**(复用本页的 `accountBucket`,它一直是对的)。**刻意不用后端的 `available`**:后端那个数答的是「中转此刻会去尝试哪些账号」,额度耗尽与令牌过期的号冷却一过仍在其中 —— 它们确实该被再试(额度会恢复、令牌能刷新),所以后端不该改;但运维看这张卡是想知道「现在有多少号是好的」,两者本就不是同一个问题。分档函数还多知道一件后端不知道的事:余额查询回来的剩余已归零 —— 那个号还没轮到就已经没额度了。

  仪表盘的「可用/总数」卡片同步改成同一口径。此前它用后端 `available`、账号页用 `!disabled`,两个页面对同一台中转显示两个不同的「可用」——修一处不修另一处,只是把矛盾从页内挪到页间。

## [0.7.8] - 2026-07-29

### Fixed

- **额度只用了 0.08 就被 402 拦死**(v0.7.6 引入的回归)。单次在途预留取 1.0 credits,而 v0.7.6 之后「已花」终于是真实值,于是一个 1 credit 的上限从第一发起就 `0.08 + 1.0 > 1.00` → 402:面板显示 `0.08 / 1.00` 还剩九成,请求却一个都发不出去。这正是 `user/handler.rs` 注释里早就警告过的情形(「上限比一次预留还小,这把 key 从签发起就发不出任何请求」),我设 1.0 时只想着保守,没算小额度会被预留本身吃光。

  预留值改为贴近实测:**credits 0.25**(实测约 0.137/次,留约一倍余量)、**USD 0.05**(实测约 \$0.0003/次,留约两个数量级以覆盖长上下文大请求)。旧值 USD 1.0 是实测的三千多倍,一个 \$1 的上限同样会从签发起就不可用。

  中途试过「按上限比例封顶预留」,**被测试否掉了**:`SpendCache` 复用快照不漏放的前提是「est ≥ 单次真实花费」,按比例砍小 est 就不再成立,超支会漏过去。测试直接暴露了这一点(第 4 次请求本该 402 却放行),故改为调整名义值、保住不变式。

## [0.7.7] - 2026-07-29

### Fixed

- **「永不过期」的密钥仍被显示成「首次使用后 1 天到期」**(v0.7.6 声称修了但没修到)。编辑表单初始化时把 `durationDays == null`(永不过期)回落成 `1`,于是每次打开这把 key,表单都预填「1 天」且「永不过期」按钮不高亮——**显示在撒谎**。后端存的一直是正确的 `null`(已用线上接口实测确认:PUT `durationDays: null` 正确清除并持久化),但只要用户在这个错误显示下点了保存,假值就变成真值。

  v0.7.6 未生效的原因值得记下:改动用 `str.replace()` 做,**没匹配上时静默跳过**,而脚本无条件打印了「已改」——那句话不是证据。文件从头到尾没被改动,提交里自然也没有它。本次改用会匹配失败即报错的方式,并从线上实拉 JS 复核改动确已生效。

## [0.7.6] - 2026-07-29

### Fixed

- **API-KEY 的 credits 用量此前是算出来的假数,限额因此形同虚设**。展示与准入闸都把 credits 写成 `花费USD ÷ 0.72`，而上游回报的**真实 credits 就在同一个聚合结构里**(`ApiKeyUsageSummary.total_credits`)被原地丢掉。全局用量那条路刻意不做这个反算(注释明写「不做 cost→credits 反算」)，两条路对同一件事给了两套算法，只有一条对。线上实测:一把设了 2.00 credits 上限的 key，面板显示 `0.00 / 2.00`，真实消耗约 1.37(已用掉七成)。更要紧的是**准入闸读的是同一个假数**——闸门看到的「已花」永远是极小值，用户设的额度上限实际拦不住任何东西，而这件事在面板上看不出来。展示、准入闸、用户面板(共 5 处)全部改用真实 credits。
- **credits 单位下的单次在途预留改为 credits 原生量**。此前写作 `EST_COST_PER_REQUEST_USD / 0.72` = 1.389 credits，那是反算时代的产物:当时「已花」也是反算的极小值，两个错配的量纲凑在一起看不出问题。改用真实 credits 后，已花是真实量级(实测约 0.137/次)，再配 1.389 的预留就成了 10 倍超额预留——一个 2 credits 的上限会在真实只用掉 0.6 时就开始 402。现取 1.0(Kiro 名义口径:一次请求 ≈ 1 credit)，仍比实测保守约 7 倍。
- **USD 用量少算了成本里通常更大的一半**。上游 meteringEvent 只回报 credits、不含 token 计量，故 token 数总是走回退:输出按响应字符数/4 估算，而**输入硬编码为 0**——等于把输入成本整个抹掉，长上下文对话尤甚，按 USD 设的上限系统性偏松。记账处请求就在手边，现复用 `count_tokens` 的统计口径估算输入 token(含工具定义与图片),不另起一套平行实现。
- **「永不过期」的密钥被静默改成「首次使用后 1 天到期」**。编辑表单初始化时 `durationDays == null`(永不过期)回落成 `1`:打开一把永不过期的 key，表单预填「1 天」、「永不过期」按钮不高亮，用户没察觉就保存，这个**错误显示当场变成真值**。新建分支另有一处不一致——选永不过期时把 `durationDays` 整个省略而非显式发 `null`，后端默认虽也得到永不过期，但两条分支对同一语义各写各的，后端哪天改默认值就只有新建这条会悄悄跟着变。两处都已改。

## [0.7.5] - 2026-07-29

### Fixed

- **账号页「失败」与「限流」两列不再张冠李戴**。`failureCount` 此前装的是 `strikes`(连续失败连击数,成功一次或进冷却即清零),`throttleCount` 装的是 `failures`(累计失败总数,与限流毫无关系)。后果:一个被上游**封禁**的账号在面板上显示成「限流 1、失败 0」——两个数都在说假话,还把「账号被停用、需联系客服」错报成「歇一会儿就好」,把排查引向完全错误的方向。现在「失败」= 累计失败数(点开即失败日志,口径一致),「限流」= 限流事件日志里的真实条数。限流条数由一次遍历得出(`counts_by_credential`),不按账号逐个扫日志。

### Added

- **面板测试进入 CI 门禁**。`admin-ui-v2/tests/` 的 33 个测试此前**一次都没在 CI 里跑过**——CI 只有 `cargo fmt`/`clippy`/`cargo test`,面板逻辑改坏了要等人工点开页面才发现。注意须逐文件传入(`node --test admin-ui-v2/tests/*.mjs`):传目录会被整个当成一个失败用例报出来。

## [0.7.4] - 2026-07-29

### Fixed

- **「重置」与「手工启停」现在立刻落盘**。v0.7.3 把封禁结论做成了持久的,但重置只改活池、不写盘:面板上点完重置,账号确实回到可用池,**下次重启又从盘上把封禁读回来**——运维明明操作过,状态却自己弹回去。封禁账号被挡在池外后永远等不到一次成功来清标签,重置是它唯一的出口,这个出口必须是持久的。手工启停是同一类问题的另一个入口(此前只改活池,靠后续某次刷新顺带带下去,中间重启一次操作就没了),一并修掉。
- **测试不再把凭据文件写进仓库根目录**。上一条给两个端点加了立即落盘,而 `set_disabled` 的测试用的是 `Config::default()` 的相对路径 `credentials.json` —— 测试跑完就在仓库根留下一份带假 token 的凭据文件,`Config::default()` 从此加载到非空池,别处「空池应回 503」的三四个测试全部变成 502。改用临时目录。

## [0.7.3] - 2026-07-29

### Fixed

- **封禁结论现在跨重启保留**。v0.7.2 让封禁账号不再计入 `available`、不再被选中,但那个结论只活在内存里:每次重启/发版都会把它抹掉,账号悄悄回到可用池,直到再失败一次才重新被挡——于是「253 个账号有 1 个封禁、可用数却是 253」会在每次重启后重现,v0.7.2 只是把复现周期从「一次冷却」拉长到「一次重启」。现在结论随 `credentials.json` 落盘(`statusReason` 键),加载时还原,与 `disabled` 同样的同步纪律。**strike 计数与冷却截止时刻仍不落盘**:那两个是计时器,重启后从零开始无非让账号早重试一次,无害;结论不同,它决定账号能不能进池。

## [0.7.2] - 2026-07-29

### Fixed

- **被上游封禁的账号不再算作可用、也不再被选中**。`available` 此前只看「未禁用 && 不在冷却」,完全不看 `statusReason`。冷却是**计时器**,到点账号自动回池;而封禁是上游给的**结论**(原话:「账号已锁定,请联系客服验证身份」),不随时间解除。后果是账号池出现自相矛盾的两个数字:面板挂着「封禁」标签,`available` 却把它算作可用(线上实测 253 个账号里 1 个封禁,可用数仍显示 253)。更实际的损害是冷却一过该账号就重新入选、必然再失败、再冷却,循环消耗真实请求。现在封禁账号不被 `select` 选中、不计入 `available`、`healthStatus` 报 `unhealthy`。
- **面板「重置」现在会清掉封禁结论**。封禁账号被挡在池外后,永远等不到一次成功来清标签(`report_success` 是唯一的清除点),而 `reset_failures` 此前只清 strikes 与冷却——不一并清掉结论的话,「重置」按钮对封禁账号就是个空操作,账号再无出口。

## [0.7.1] - 2026-07-28

### Fixed

- **Responses 接口不再因内置工具整轮被拒**。`/v1/responses` 的工具定义此前把 `name` 设成必填,而照 OpenAI 规范,工具数组里除了 `type:"function"` 还混着内置工具(`web_search` / `local_shell` / `file_search` …),这类工具**天生没有 `name`**。真实客户端(codex)一次送十几个工具,其中一个内置工具就足以让整轮请求死在反序列化:`Failed to deserialize the JSON body: tools[13]: missing field \`name\``——错误只报下标,看不出是哪类工具,接入方无从下手。现在 `name` 可缺省;能映射到中枢的(带 `name` 的 `function` / `custom`)照常转换,内置工具由 OpenAI 服务端自行执行、Kiro 数据面无等价物,故丢弃并落一条 WARN(`responses_builtin_tool_dropped`,带 `tool_type`)——不记的话,「模型怎么不会搜网页」在日志里毫无线索。
- **Responses 多轮不再因无法映射的输入条目整轮被拒**。`input` 里遇到不认识的条目类型此前直接判错。但客户端做多轮时会把上一轮的**整个** output 原样回灌,里面必然带 `reasoning`、`local_shell_call` 这类 Responses 侧产物,中枢没有对应槽位——判成错误的后果是**第一轮能通、第二轮必炸**,且错误只说「不支持的条目类型」。现在这类条目整条跳过(记 DEBUG `responses_input_item_skipped`),可映射的条目不受影响。
- **Responses 函数工具允许省略 `parameters`**。中枢要求 `input_schema` 是 JSON Schema 对象,此前直接透传 `null`,会被上游判成畸形工具、整轮失败;现在补成空对象 schema。

## [0.7.0] - 2026-07-28

### Added

- **令牌刷新失败现在会记录原因,并进入账号状态**。此前刷新失败被完全吞掉:日志里只有「令牌即将过期,刷新中」紧接着「跨账号重试」,中间那句**为什么失败**整个消失。线上真实事故:上游对整批账号回 `access_denied`,面板上只表现为「账号全过期了」——运维无从判断是账号被上游处置了、还是中转自己坏了,只能手工拿凭据 curl 上游才问得出来。现在失败即落一条 WARN(`token_refresh_failed`,带上游状态码与响应体摘要),并把原因写进 `statusReason`。
- **新增 `refresh_denied`(续期被拒)状态档**。`access_denied` / `invalid_grant` 这类**刷多少次都没用**的拒绝,与「令牌过期、刷一下就好」严格分开——混在一起会让运维一直等一批永远不会自愈的账号。账号页筛选下拉新增「续期被拒」一档,徽章为红色。
- **账号页新增「全选本页」与「批量禁用」**。此前只有批量删除/重置/验证,禁用得一个个点。「全选本页」刻意只选**当前页 + 当前筛选**下可见的账号,而不是全部——一键选中上千个看不见的行再批量删除,后果不可挽回;要跨页操作请先用筛选缩小范围。「全选本页」同时放在工具栏和批量条上:批量条只有选中后才出现,而「一个都还没选」恰恰是最需要全选的时刻。批量禁用只对当前仍启用的账号发请求。


## [0.6.0] - 2026-07-28

### Added

- **账号列表静默自动刷新(30 秒)+ 新鲜度提示**。此前账号页在**打开那一刻拉一次之后就冻住**:账号被上游封禁、冷却结束恢复、令牌过期,面板全都不会变,除非手动点刷新或离开再回来。运维照着一屏过时的徽章做判断,比没有徽章更糟——尤其 v0.5.0 新加的状态筛选,「封禁账号 (0)」这种数字看着像结论,其实可能是十分钟前的。
  - 只重拉**列表接口**(读进程内存,便宜);**绝不**顺带重跑余额扇出——那是逐个打上游的,每 30 秒来一遍等于给上游送风控。余额仍由「查询信息」按需触发。
  - 静默刷新**不重置页码、不清空筛选、不动选中态、保留滚动位置**;失败不弹提示,下一拍再试。
  - 工具栏显示「刚刚更新 / N 秒前更新 / N 分钟前更新」。**即便刷新链路断了,运维也能一眼看出屏幕上的数字是几分钟前的**,超过两拍未成功即转为警示色,不让陈旧快照冒充实时数据。
  - 标签页切到后台即暂停(浏览器本就会节流甚至冻结定时器),切回前台立刻补一次——那正是运维开始看它的时刻。离开账号页或 401 掉线时停掉定时器,避免每一拍都再撞一次 401、把登录浮层反复重弹。


## [0.5.1] - 2026-07-28

### Fixed

- **健康徽章与状态筛选各算各的,结论互相矛盾**。筛选走 v0.5.0 新加的分档,徽章却仍只看 `healthStatus` —— 于是「过期账号」那一档里的行照样挂着绿色的「健康」徽章。现在徽章由同一个 `accountBucket` 驱动,两处不可能再打架;「异常」这一档内仍按 `healthStatus` 细分成警告(有失败但仍可选)/ 异常(冷却中),信息量不减。封禁 / 已过期 / 额度耗尽三档新增对应徽章文案(5 语言)。
- **余额已耗尽的账号仍显示为健康**。`额度耗尽` 此前只认 `statusReason === 'quota'`,而那要等到该账号真的被选中、真的失败过一次才会被标上。账号还没轮到就已经没额度的情况完全覆盖不到。现在余额查询回来的**剩余归零**同样判为额度耗尽 —— 两条来源缺一不可。注意「还没查过余额」与「余额为 0」严格区分:前者不改变分档(不知道就不猜)。
- **查完余额后徽章与筛选条数不刷新**:扇出此前只改了那一行的「剩余」数字,于是屏幕上会出现剩余显示 `0`、徽章却还是绿色「健康」的一行。现在每条余额回来即就地重算该行徽章并刷新下拉里的条数(不整表重绘,避免把滚动位置和选中态冲掉)。


## [0.5.0] - 2026-07-28

### Added

- **账号状态筛选**:账号管理页「查询信息」左侧新增下拉,7 档——全部账号 / 健康账号 / 异常账号 / 禁用账号 / 封禁账号 / 过期账号 / 额度耗尽。每档文案带**实时条数**,不用点进去就能看出有多少号被封、多少额度耗尽。分档按优先级取第一个命中(禁用 → 封禁 → 额度耗尽 → 过期 → 异常 → 健康),依据是运维要采取的动作:封禁要换号,额度耗尽要等重置,单纯冷却等一会儿即可。切换筛选自动回到第一页;文案 5 语言齐全。
- **`StatusReason`:把「异常」拆开**。上游停用账号时响应体带 `suspend` 字样,代码原本识别它,但只用来决定「别永久禁用、让它冷却」,分类完这个信息就丢了——于是封禁、额度耗尽、令牌过期、限流在面板上全挤在一个「异常」里,列表上分不出来。现按上游原始响应体归类为 `banned` / `quota` / `token_expired` / `throttled`,经 `GET /api/admin/credentials` 的新字段 `statusReason` 透出。
  - **封禁判定优先于限流**:上游停用账号时响应体常常同时带限流措辞,先匹配限流会把封禁误报成一次普通限流,那批号就会一直被当作「等等就好」。
  - **分类只进展示层**:不改变选号纪律、冷却时长与禁用判定。分错了最多让标签不准,绝不能让一个健康账号因为措辞匹配被判死。
  - 账号**成功一次即清空标签**,否则已经恢复的号会一直挂在「封禁」档。


## [0.4.0] - 2026-07-28

### Changed

- **协议侧 `/models` 现在列出全部 17 个可服务模型,三个协议结果一致**。此前 `GET /v1/models`、`GET /claude/v1/models`、`GET /v1beta/models` 各自硬编码**三条**,而且三组**互不相同**(OpenAI 列 `claude-sonnet-4.5` / `claude-opus-4.6` / `gpt-5.6-sol`,Anthropic 列三条 claude,Gemini 又是另一组),管理接口却有 17 条。于是客户端"先列模型、再按列出的 id 调用"这条标准流程在本服务上不成立:列出来的只是残缺子集,换个协议看到的还不一样。现新增 `src/models_catalog.rs` 作为唯一目录,四个端点(三协议 + 管理侧静态回落)同源。测试保证目录里每个 id 都能被 `map_model` 识别(否则就是列出一个"调用即 400"的模型)、三协议逐项一致、id 不重复。
  - 管理接口 `GET /api/admin/models` 行为不变:缓存有上游并集时仍优先回并集,本目录是它的回落项。协议侧一律用目录而不取并集 —— 客户端的模型发现需要稳定可预期的结果,不应随缓存冷热在重启前后变化,也不该为一次 `/models` 给账号池加压。

### Documentation

- 补齐此前从未写进 API 参考的 12 个线上路由:`/api/admin/restart`、`/api/admin/update`、`/api/admin/check-update`、`/api/admin/credits/global`、`/api/admin/usage/summary`、`/api/admin/usage/daily/{date}/records`、`/api/admin/credentials/{id}/throttle-logs`、`/api/admin/credentials/{id}/usage/today`、`/api/admin/credentials/models/refresh`、`/api/admin/credentials/{id}/models/refresh`、`/api/user/usage/records`,以及 `/admin/api/*` 旧别名组。
- 五套 `API.md` 中关于协议侧 `/models` 的描述全部按上述变更重写(此前写作"固定短清单",并建议改用管理接口查真实可用模型 —— 该建议已不再必要)。


## [0.3.1] - 2026-07-28

### Fixed

- **`POST /v1/messages/count_tokens` 的畸形请求体回 axum 默认的纯文本 422,而不是 Anthropic 错误体**。v0.3.0 把四个协议对话端点都改成了显式接管拒收(回各自形状的 400),唯独漏了这个同属 Anthropic 协议、同样由 SDK 直接调用的端点。SDK 用 `response.json()` 读错误时,纯文本体只会抛解析异常,真正的失败原因被吞掉;且 422 也不在 Anthropic 的错误码契约里。

### Documentation

- 五套 `API.md` 的状态码表同步:`/v1/messages/count_tokens` 从"回 422"一栏移出——它现在与四个对话端点一致回 400。

## [0.3.0] - 2026-07-28

对 v0.2.1 自己的修复做了一轮独立复查:那一轮确认为真的 39 条里,**9 条只关掉了一部分**却被写成了已完成;另有 **13 条候选从未被裁决**(复核者中途崩溃,当轮直接按"39 条"继续),其中 12 条经复核确属真实缺陷。本版把这 21 处全部关掉。

复查同时暴露了一个方法问题:上一轮给"凭据绑定"写的回归测试断言的是**值有没有传到请求扩展**,而不是**选号有没有照它执行**——所以功能形同虚设,测试却全绿。本版为每条修复补的测试都先在修复前的代码上跑过、亲眼看它失败,才算数。

### Security

- **API-KEY 的凭据绑定此前完全没有生效**。v0.2.1 的发版说明称其"现已真正生效",这句话不成立:鉴权闸确实把白名单解析出来并塞进了请求扩展,但**全仓库没有任何非测试代码读取它**,选号内核只按 启用/冷却/RPM/已试集 过滤。绑定到 3 号账号的 key 照样会被分到池里任意账号,四个协议、流式与非流式都是如此。现在白名单穿过整条中转链下达到唯一的选号入口,成员判定统一走 `BoundCredentialIds::allows`(fail-closed:凭据 id 解析不出数值一律不放行);绑定命不中任何账号时回 503,而不是回落到未授权账号。
- **客户端 IP 仍可伪造**。此前取 `X-Forwarded-For` 的**最左**项,而最左恰恰是调用方自己就能写死的那一项——反代只会把它**亲眼看到的**对端追加到最右。于是任何持有效 key 的调用方都能决定自己在用量记录、失败日志与面板上被记成哪个 IP。现按新增的 `trustedProxyHops`(默认 1 = 前面只有一层自己的反代;0 = 无反代、一律不采信转发头)从右往左定位可信那一跳。CDN 权威头(`CF-Connecting-IP` / `True-Client-IP`)降为"压根没有 XFF 时"的兜底——Caddy、nginx 这类反代会把调用方自带的同名头原样透传,把它排在 XFF 之前等于留了一条绕过通道。转发头的值现在必须解析成合法 IP 才采用,否则丢弃:此前只做 `trim()`,任意字符串(控制字符、超长文本、伪造成日志格式的片段)都能原样写进审计字段。

### Fixed

- **凭据文件损坏时的 id 抢救只做了一半**:`api_keys.json` 被手工编辑出尾逗号、或写到一半被截断时(整份文件不再是合法 JSON),抢救逻辑在解析失败处直接返回空结果、`next_id` 归零,于是**下一把新建的 key 拿到 id=1**,而用量账本是另一个文件、并不会跟着损坏——新 key 当场继承前任的调用明细与累计消费(可能一上来就 402)。现增加原始字节层的抢救:扫出所有完整的条目对象并逐个恢复,同时从幸存字节里取 `next_id` 与全部 `id` 的上界,确保已被用过的 id 绝不回收。
- **停机刷盘只覆盖了三个存储中的一个半**:余额缓存与失败/限流事件日志仍会丢掉最后约 5 秒。余额缓存尤其要紧——删除账号时的 `invalidate` 一旦丢失,复用同一 id 的新账号会显示已删账号的余额;而重启后丢失的缓存意味着要对上游重新查一遍。
- **API-KEY 页的 RPM 恒为 0**:`GET /api/admin/rpm` 的 `byApiKey` 无条件返回空对象。账号页那一半在 v0.2.1 修好了,密钥页这一半没有。
- **删除账号不清理用量记录**:记录无条件残留,id 复用后即产生跨用户的归属污染。
- **已用完额度的 key 每发一次请求仍会全量扫描用量账本**。这是永久状态(消费上限按累计计),而这些请求本来就会被 402 拒掉,扫描纯属浪费且可被人为打爆。
- **用户面板显示"正常"而中转已经在回 402**:鉴权闸准入时会为在途请求预留一笔估算费用,面板却按 `已用 >= 上限` 自己另算一套,于是顶部那一段区间里徽标是绿的、进度条还有余量,而每一发请求都被拒。现在由服务端下发与闸门同一算式的判定结果。
- **上游错误体从未落库**:响应体读出来做完分类就被丢弃,`record_classified_failure` 收到的恒是空串,于是面板上失败/限流的"详情"列线上永远是 `—`。唯一覆盖它的测试是手工注入响应体的,生产链路根本产生不出非空值。
- **账号页的余额扇出竞态**:自动扇出在自增取消令牌**之前**就返回,重载账号列表无法真正取消在飞的那一轮。
- **OpenAI Responses 的畸形请求体**回 axum 的纯文本 422 而非协议错误体(v0.2.1 只修了 `/v1/chat/completions`)。

### Documentation

- 六套文档(5 语言 + 根 README)经三轮交叉复查,凡与代码不符的契约全部更正:请求字段名(`enabled` 而非 `disabled`、`bearerToken` 而非 `tokens`)、路径参数类型、鉴权闸实际接受的**六条**凭证通道(此前各处写成"两种"或"三选一")、状态码表(四个中转端点把请求体解析失败转成协议形状的 **400** 而非 422;超过本地 RPM 上限**永远不会**回 429,只会让账号不可选、全灭时回 503)、响应示例里服务端根本不会下发的字段、被写成可变而实为硬编码的值、协议侧 `/models` 的真实内容、批量导入的响应形状,以及"`server-info` 已脱敏"这一条——它返回的是**完整明文**主密钥。
- 三个**文档写了但根本不生效**的请求参数改为如实标注为"接受但忽略":`temperature`(压根不是任何请求结构体的字段,反序列化时被当作未知键丢弃)、`max_tokens`(解析进中枢但从未放上上游线格式,不会截断回答;`finish_reason: "length"` 反映的是上游自己的预算)、`tool_choice`(带进中枢后再没有任何地方读取,强制指定工具在所有协议上都无效)。

## [0.2.1] - 2026-07-27

在 v0.2.0 之后又做了一轮 10 维度对抗式排查:提出 67 条疑点,逐条交给"以推翻为职责"的复核者,**39 条被确认为真**并在此修复(含前端与文档——此前从未被审计过)。

### Security

- **含密文件不再被写成全局可读**:`api_keys.json`(全部用户 API-KEY 明文)与 `config.json`(主密钥 + 管理员密钥明文)此前经 `File::create` 落盘,权限取 `0666 & ~umask`(通常 0644);**即使运营者手动 `chmod 600`,下一次刷盘又会改回去**(线上实测复现)。现所有原子写建临时文件即 `0600`,rename 保留该权限。
- **客户端 IP 不再可伪造**:`CF-Connecting-IP` / `True-Client-IP` / `X-Forwarded-For` 此前被无条件采信,任何能直连服务端口的客户端都能决定自己被记成哪个 IP(污染用量记录、失败日志与面板)。现仅在对端为回环/私网(反代回源)时采信。
- **API-KEY 的 `boundCredentialIds` 现已真正生效**(此前存了、面板也显示,但从不校验)。
- 修正 API 文档与 rustdoc 中"管理接口响应绝不含密钥""`masterApiKey` 已脱敏"的失实描述——二者均为完整明文,现已如实标注并给出脱敏端点。

### Fixed

- **`GET /api/admin/models` 的全池扫描**此前无上限、无去重、无单飞:模型清单缓存只在内存,重启即空,于是**每次打开仪表盘**都会遍历整个账号池;在千账号且令牌普遍过期时,单次访问即触发上千次令牌刷新与上游调用、重写数 GB 凭据文件,上游不可用时更会无限重来并把整池打进冷却。现改为单飞 + 有上限 + 冷却 + 轮转游标。
- **凭据文件损坏不再静默清空**:`credentials.json` 解析失败此前被当作空池,随后一次管理写入即原子抹掉全部账号。现先备份、再逐条抢救(单条坏数据不再拖垮其余)、并以 error 级别明确告警;`api_keys.json` 同理。
- **停机不再丢弃 API-KEY 变更**:此前仅刷用量统计,刚吊销的 key 会在重启后复活。
- **OpenAI 并行工具调用**被拆成连续用户轮,导致声明了两个 `tool_use` 的助手轮只跟一个 `tool_result`,工具往返不合法。
- **Gemini 兼容**:含 `googleSearch`/`codeExecution`/`urlContext` 的 `tools` 数组此前使整条请求 422;snake_case 请求键(`system_instruction`/`generation_config`/`inline_data` 等)被静默丢弃;非图片 `inlineData`(PDF/音频/视频)被当作图片附上错误的 format;请求体畸形时返回 axum 的纯文本而非 Gemini 错误对象。
- **协议路由仍受 axum 2 MB 默认体积上限约束**,约 1.5 MB 的图片经 base64 膨胀后即被拒;现与管理组一致提升上限。
- 余额端点此前每次请求都用阻塞 I/O 重读并解析整个凭据文件,仅为做一次存在性判断;设了消费上限的 key 每次请求都要全量重扫用量账本。
- 账号 id 高水位此前用裸 `fs::write` 落盘,每次令牌刷新都会把它截断;未校验的 `durationDays` 可溢出并使鉴权层 panic。
- **管理面板**:被拒绝的登录(403)不再销毁管理会话;保存失败不再提示"已保存";接口失败不再显示伪造的 0;仪表盘冷启动不再对全部账号无节流并发查余额,401 后也不再循环清空管理密钥;模型测试的密钥下拉不再被永久禁用;账号页 RPM 现已真实拉取;失败日志详情不再恒为"—"。
- **用户面板**:登出时清空查询缓存(此前下一把 key 会看到上一位用户的数据);密钥失效时会真正退出而非一直显示过期数据与"正常";`spendingLimit = 0` 不再被当成"无限制";未激活的按时长密钥现会显示有效期;地理位置查询改走 https(此前在 https 页面被混合内容拦截)。

## [0.2.0] - 2026-07-26

一轮覆盖事件流、账号池、统计、登录、四协议、鉴权与部署的全链路审计与修复。改动面较大且含行为变更,故跳次版本号。

### Security

- **修复消费上限绕过**:给 API-KEY 设的消费上限此前**只在 Anthropic 端点生效**——OpenAI / OpenAI-Responses / Gemini 三条协议记账时把归属硬编码为 0,换用这些端点即可无限消费且永不 402,面板上这些流量的用量与花费还全部显示为零。现四协议一律按鉴权闸解析出的 key 归属并按真实计量结算。
- **修复管理面无鉴权开放**:仅配置了用户级 API-KEY(未配 `apiKey`/`adminApiKey`)时,`/api/admin/*` 此前对匿名放行——可读出全部用户 key 明文、抢改鉴权密钥、任意增删凭据、反复重启造成停服。现管理面只认管理员级凭据(`adminApiKey`,缺省回退 `apiKey`);同时**不**把用户级 key 计入"已配置鉴权",否则首次部署的运营者一建出第一把 key 就会把自己永久锁在门外。
- `credentials.json` 等含密文件的原子写临时文件改为 **0600** 创建(此前继承 umask 通常为 0644,而文件里是全部账号的明文令牌)。
- 空环境变量不再覆盖 `config.json`:`.env.example` 自带的 `API_KEY=` 经 compose 注入后会静默关掉鉴权。
- store key 校验改常量时间比较;API-KEY 编号改单调递增并持久化(编号复用会让新 key 继承前任的用量、真实 IP 与累计消费)。

### Fixed

- **上游错误不再被伪装成正常完成**:①上游在 HTTP 200 事件流里下发的 `exception` 帧此前被整体忽略,客户端拿到一个"内容为空/半截却正常结束"的响应,重试逻辑永不触发,该次失败还被计入成功用量;②**传输层中断**(连接重置 / 读超时 / chunked 体未收尾)同样落进正常收尾分支。现四条协议一律以各自规范的错误事件收束(Anthropic `error` 事件、OpenAI 错误 chunk 且不补 `[DONE]`、Responses `response.failed`、Gemini 错误块且绝不报 `STOP`),超时映射 504、其余 502。
- **截断不再被报成正常结束**:命中 `max_tokens` 或上下文耗尽时,流式路径此前一律报 `end_turn`/`stop`/`STOP`/`completed`;现与非流式同口径(`max_tokens` / `length` / `MAX_TOKENS` / `incomplete`)。
- **账号池**:刷新失败此前完全不反馈给池,`refresh_token` 已吊销的账号永远显示健康、每次被选中都白打一轮注定失败的刷新;现按 token 端点语义分类(仅 400/401/403 + `invalid_grant` 才判永久失效,其余一律降级为可自愈冷却,避免一次 5xx 或网关错误页永久禁用健康账号)。启停状态在内存与落盘两处同步;强制刷新以"调用方实际失败的那枚令牌"为基线,迟到者不再把别人刚换好的令牌再轮换一遍。
- **统计**:后台刷盘任务与脏标记构成 `Arc` 自引用环,导致"关闭时兜底刷盘"是死代码,每次重启静默丢最近约 5 秒的用量与计费;每日汇总与原始记录同源同被淘汰,使 7d/30d 汇总静默偏低;载入时用 `.ok()` 吞掉解析错误,损坏文件被当空库并在数秒后原子覆写。均已修复,并新增停机刷盘入口。
- **登录**:一次网络抖动或代理塞回的 HTML 错误页此前会销毁整个登录会话(用户即使已在浏览器点过授权也得从头再来);现区分瞬态与终态,设备码轮询按 OAuth 错误码判定,SSO 令牌兑换改为带上限的退避轮询。
- **事件流解码**:帧内 headers 段自截断会让解码器永久停滞并丢弃其后全部合法帧(非流式路径表现为响应截断 + 计费丢失);resync 预算耗尽时会连噪声之后已到达的合法帧一起清空。均已修复,并给 headers 段加上条数上限。
- **部署**:`--credentials` 此前解析了却从未使用,照文档部署后容器显示 healthy 但四协议全部 503,凭据与统计还写在容器可写层、升级即丢;健康检查只读 `config.json` 的端口,按文档改 `PORT` 后容器永远 unhealthy。均已修复。
- 修复纯 HTTP 访问下更新弹窗复制按钮无反应(非安全上下文回退 `document.execCommand('copy')`)。
- Gemini:`functionCall`/`functionResponse` 的 id 配对改为"回带 id 精确配对、缺 id 配最近一轮未被答的同名调用",消除并行同名调用被并成一条、以及工具结果成孤儿导致模型反复重发同一调用的问题;`streamGenerateContent` 支持 `alt=sse` 与默认 JSON 数组两种线格式。
- OpenAI:`system`/`tool` 角色的数组形 `content` 此前被替换成空字符串(工具结果整体丢失);`developer` 角色未映射为 `system`;Responses 的 `input` 条目此前把 `type` 设为必填,官方 SDK 的常见写法与多轮回灌都会 422。均已修复,并补上流式 `usage` 与 `stream_options.include_usage`。

### Changed

- **优雅停机加排空上限**:管理面实时日志 SSE 是无界长连接,此前会让停机一直等到容器宽限期结束被强杀(在途流被掐断、最终刷盘跳过);现最多等待 8 秒即刷盘退出。
- **统计磁盘格式保持可回滚**:`usage_records.json` 恒为裸数组(旧版本可原样解析),新增的每日汇总落在旁挂的 `usage_records.daily.json`。
- **凭据路径的内置默认改为就近解析**到 `-c` 所指配置文件的目录(容器因此仍默认落在挂载卷内),镜像不再烘焙 `CREDENTIALS_PATH` 环境变量——环境变量层优先级高于 `config.json`,烘焙会把用户自定义的路径静默改道。
- 账号 id 改单调递增并持久化,重启后不再复用已删除账号的编号。
- 文档修正:`/api/admin/*` 只有配置了 `adminApiKey`(缺省回退 `apiKey`)之后才受保护,`/api/user/*` 不走该闸、始终要求调用方自带有效 API-KEY;`logCapacity` 默认值、配置优先级补上命令行层。

## [0.1.4] - 2026-07-26

### Fixed

- **Anthropic `system` 字段兼容内容块数组**:真实客户端(Claude Code、带 prompt 缓存的 SDK)会把 `system` 发成 `[{"type":"text","text":"…","cache_control":{…}}]` 数组,之前只接受字符串会返回 422(`invalid type: sequence, expected a string`)。现在 `system` 同时接受字符串与内容块数组两种形态,转发到 Kiro 后端与 `count_tokens` 时拍平为纯文本(OpenAI/Gemini/Responses 前端不受影响)。

## [0.1.3] - 2026-07-26

批量导入 JSON 的实时化改版。

### Changed

- **批量导入 JSON 改为实时逐条验活显示**:导入对话框现在逐个账号处理并实时展示——进度条 +「正在处理账号 i/N」+ 成功/重复/失败实时统计 + 每个账号一行的状态列表(等待中 → 检查重复 → 验活中 → 验活成功[带用量] / 重复账号 / 验活失败[已排除])。验活通过的账号即时落库,中途中断也不丢失;导入进行中不可关闭对话框。(细化 v0.1.2 的「逐条验活 + 去重」导入。)

## [0.1.2] - 2026-07-25

检查更新弹窗改版,以及文档补全。

### Changed

- **检查更新弹窗对齐 gemini2api**:仪表盘加载时静默自检,有新版本时「检查更新」按钮高亮为「更新到 vX」;点击弹出「更新服务 vX」对话框,内含**当前界面语言**的发布说明(可滚动)与升级命令(`docker compose pull && docker compose up -d`,一键复制)。仅提示展示,不自动升级。
- **文档补全(此前 v0.1.1 遗漏)**:README(根 + 5 语言)「最近更新」表补上 v0.1.1 / v0.1.2 行、功能清单补「模型测试」与「批量导入(逐条验活 + 去重)」;USAGE(5 语言)新增「模型测试」小节、扩写「检查更新」与「批量导入」说明。

### Fixed

- 修复纯 HTTP(非 HTTPS)访问下更新弹窗的复制按钮无反应:非安全上下文 `navigator.clipboard` 不可用时回退 `document.execCommand('copy')`。

## [0.1.1] - 2026-07-25

面板体验与账号导入的一批修复,以及文档/构建完善。

### Added

- **模型测试默认可用主 API-KEY**:未创建任何自建密钥时,模型测试页默认列出并使用「主 API Key」,开箱即可测通。
- **批量导入账号「逐条:添加 → 验活 → 过滤」**:导入 JSON 时逐个账号单独添加,添加后查一次余额(`getUsageLimits` 真打上游)做**验活**——查得到=有效保留,报错=失效自动回滚删除,过滤掉已失效账号。
- **导入去重**:按 `refreshToken` 跳过已在池中的账号,避免同一账号被重复导入产生两条抢同一轮换令牌的凭据(刷新时互相作废、浪费配额、增加上游风控)。
- **用户面板 `/user` 与全部页面的品牌 favicon**;多语言 README 顶部加入 128×128 品牌 logo 与 version 徽章;`scripts/set-version.sh` 一处改动同步 VERSION / Cargo.toml / 各语言徽章。

### Changed

- **批量导入不再一次性提交超大请求**:改回旧版逐条添加(每个请求都小),从根上避免大批量导入触发请求体积上限而报「发生错误」。
- **镜像构建改为交叉编译**:多阶段 Dockerfile 用 `$BUILDPLATFORM` 原生工具链交叉编译 arm64(替代 QEMU 模拟),多架构镜像构建显著提速。

### Fixed

- 修复未创建密钥时模型测试无可用密钥可选的问题。
- 修复批量导入 JSON 在账号较多时报「发生错误」(请求体积超限)。
- 修复用户面板 favicon 未随管理面板一并更新的问题。

## [0.1.0] - 2026-07-25

首个公开版本。多协议 AI 中转，后端为 Kiro（CodeWhisperer），统一提供 Claude 系模型。

### Added

- **四协议前端**：OpenAI Chat（`/v1/chat/completions`）、Anthropic Messages（`/v1/messages`，中枢母格式）、OpenAI Responses（`/v1/responses`）、Gemini 原生（`/v1beta/models/{m}:generateContent`）；每协议同时挂标准裸前缀与显式厂商前缀（`/openai/v1`、`/claude/v1`、`/gemini/v1beta`）。
- **完整能力**：每协议均支持流式（SSE）、函数调用（工具）真透传、图片输入（多模态）。
- **Kiro 账号池**：多账号轮询（`priority`/`balanced`）、每账号 RPM 限流、分级冷却；body-aware 失败分类（真凭据失效才永久禁用，配额/风控/限流冷却自愈）。
- **令牌自愈**：token 到期单飞刷新 + 原子落盘 `credentials.json`；三种交互式登录流（Builder ID 设备码 / IAM SSO 授权码 / 社交令牌）。
- **端点回退与跨账号重试**：Kiro IDE → CodeWhisperer → AmazonQ 按序回退；账号级失败跨账号重试；确定性请求错误（`INVALID_MODEL_ID`）不重试、不误伤账号，直接以 400 回明确说明。
- **统一鉴权闸**：`Authorization: Bearer` / `x-api-key` / `?token=` 常量时间比较；`adminApiKey` 保护 `/api/admin/*`，持有者以自身 API-KEY 访问 `/api/user/*`；`/health`、`/v1/ping` 不鉴权。
- **Web 管理面板 `/admin`**：仪表盘（运行时间/全局积分/系统信息/赞助卡/检查更新）、账号管理、API-KEY 管理、用量统计（含客户端 IP 与账号标签）、实时日志（SSE）、设置（负载均衡/鉴权密钥/集成示例/一键重启）；顶部运行状态、GitHub、重启、深浅色主题、5 语言 i18n。
- **用户面板 `/user`**：持有者以自身 API-KEY 登录，查看额度、累计用量与分页记录。
- **统计与缓存**：每日/账号用量统计、失败/限流日志、账号余额缓存（TTL）、动态模型清单缓存。
- **版本检查 / 更新 / 重启**：`GET /api/admin/check-update`（GitHub Release 比对）、`POST /api/admin/update`（返回更新命令）、`POST /api/admin/restart`（二次确认）。
- **交付**：多阶段 Docker 构建、非 root 运行（gosu）、多架构镜像（amd64/arm64）、健康检查、CI（tag 触发 GHCR 发布）。
