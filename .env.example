# 鉴权
# 主 API key(留空则需在 config.json 配置;为空启动会报错)
API_KEY=
# 管理端 key(留空则回退用 API_KEY)
ADMIN_API_KEY=

# 服务
# HOST 留空即用安全默认 127.0.0.1;容器镜像已内置 ENV HOST=0.0.0.0,无需在此设置。裸机部署请勿轻易改 0.0.0.0(当前 /admin、/user 尚未接鉴权)。
# HOST=0.0.0.0
PORT=8080
REGION=us-east-1
