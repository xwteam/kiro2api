name: CI
on:
  push:
    branches: [main, "feat/**", "fix/**", "chore/**"]
  pull_request:
  workflow_dispatch:
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: 构建 user-ui/dist (rust-embed 编译期嵌入;与 Dockerfile 前端阶段一致)
        run: |
          npm install -g pnpm@9.15.4
          cd user-ui
          pnpm install --frozen-lockfile --ignore-scripts || pnpm install --no-frozen-lockfile --ignore-scripts
          pnpm build
      - name: fmt (阻塞)
        run: cargo fmt --all -- --check
      - name: clippy (阻塞)
        run: cargo clippy --all-targets -- -D warnings
      - name: test (阻塞)
        run: cargo test --all
      # 面板逻辑此前完全没有门禁:33 个 admin-ui 测试一次都没在 CI 里跑过,改坏了要等人工点开面板才发现。
      # 逐文件传入而非传目录:`node --test <目录>` 在本仓会把整个目录当一个失败用例报出来。
      - name: 面板测试 (阻塞)
        run: node --test admin-ui-v2/tests/*.mjs
